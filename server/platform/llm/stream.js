export async function streamAgentThroughPlatform(deps, options) {
  return deps.streamAgentResponse(options);
}

export async function streamAgentEThroughPlatform(deps, options) {
  return deps.streamAgentEResponse(options);
}
