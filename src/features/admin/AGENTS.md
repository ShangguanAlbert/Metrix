# admin feature 协作说明

本文件适用于 `src/features/admin/` 目录及其子目录。

## 职责范围

- 管理员侧页面与管理后台入口
- 当前主要承载管理员设置、在线用户、课堂配置等页面

## 修改原则

- 保持 `routes.js + pages/` 的简单结构
- 管理员权限由路由 `auth: "admin"` 控制；不要在页面里重复实现另一套路由权限逻辑
- 若新增管理页，优先沿用现有 `/admin/*` 路径风格

## UI 约定

- 尽量复用已有管理后台样式和页面结构
- 不要把管理员专属逻辑泄漏到普通用户 feature
